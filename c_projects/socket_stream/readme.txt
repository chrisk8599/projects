Create a networked server (“JXServer”) that sends files to clients in
response to requests. The server supports multiple connecting clients simultaneously as well as
multiple simultaneous connections from the same client for increased transfer speeds.
